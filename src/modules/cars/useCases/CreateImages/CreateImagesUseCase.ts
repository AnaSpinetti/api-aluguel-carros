import { injectable } from "tsyringe";

interface IRequest {

}

@injectable()
class CreateImagesUseCase{
    constructor(
        @injectable()
    ){}

    async execute():
}

export {CreateImagesUseCase}